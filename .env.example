# Backend Environment Variables
# ================================

# Node Environment
NODE_ENV=development

# Database Configuration
# Example: postgresql://username:password@localhost:5432/taskmanager
# For development with local PostgreSQL:
DATABASE_URL=postgresql://postgres:your_password@localhost:5432/taskmanager

# Server Configuration
PORT=5000

# JWT Secret (change this in production!)
ACCESS_TOKEN_SECRET=your-super-secret-jwt-key-change-this-in-production-min-32-chars

# CORS Configuration
FRONTEND_URL=http://localhost:3000
# For production: https://your-frontend-domain.com

# Email Configuration (for notifications)
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-specific-password

# Email Service Provider (gmail, sendgrid, etc.)
EMAIL_SERVICE=gmail

# Optional: Logging
LOG_LEVEL=info

# Database Connection Pool (Sequelize)
# These are set in database.js but can be overridden here if needed:
# DB_POOL_MAX=10
# DB_POOL_MIN=2
# DB_POOL_ACQUIRE=30000
# DB_POOL_IDLE=10000
